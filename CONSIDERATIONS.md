# 고려사항 문서

이 문서는 과제 원문(과제_파일업로드_AI개발.md) 3장의 항목 순서를 그대로 따릅니다.  
각 항목마다 구현 여부와 판단 근거를 남겼습니다.

---

## 검증/보안 관점

### 확장자만 믿어도 되는가? [매직 넘버 검증]
**구현완료**  
`FileSignatureDetector`가 업로드된 파일의 **콘텐츠 바이트**만으로 실제 파일의 형식을 판별하게했습니다.
클라이언트가 보낸 `Content-Type`(MIME)은 신뢰할 수 없다고 판단하였습니다.

문제는 매직 넘버만으로 확장자 하나를 정확히 특정할 수 없는 경우가 있었습니다.
- `exe/com/scr/cpl`은 전부 동일한 Windows PE(`MZ`) 헤더를 쓰기떄문에 콘텐츠만으론 서로 구분 불가능
- `js/bat/cmd` 등 텍스트 스크립트는 매직 넘버 자체가 없음
- `docx/xlsx/pptx`는 내부적으로 ZIP 컨테이너라 순수 zip과 시그니처가 같음

**판단**: 이 세 "계열"에 대해서는 콘텐츠가 그 계열에 속함을 확인한 뒤, 파일명이 주장하는 확장자가 계열의 합리적 멤버면 인정하고 계열 밖이면 대표값(PE→`exe`, 텍스트→`txt`, zip→`zip`)으로 판정해 위장 검사에서 걸리게 했습니다. 과제가 예시로 든 "`report.jpg`인데 실제로는 실행 파일"이 정확히 이 로직(PE인데 claimed가 PE계열 밖)으로 필터링 되도록 했고, 통합테스트로 검증을 완료하였습니다.

계열에도, 개별 시그니처(PNG/JPG/PDF 등)에도 해당하지 않는 **완전히 미지의 바이너리**는 claimed를 그대로 인정하도록 했습니다. 그렇지 않으면 시그니처를 등록해두지 않은 모든 정상 포맷이 위장으로 오판되어 차단되는데, 이는 과제가 요구한 [확장자 블랙리스트 모델]을 [아는 포맷만 허용하는 화이트리스트 모델]로 바꿔버리는 과도한 차단이라고 판단했습니다.

### 대소문자 / 이중 확장자
**구현완료**  
- 대소문자: 확장자는 저장/비교 시 항상 소문자로 정규화하는 로직을 구현했습니다.(`normalize()`, `FilenameAnalyzer`).
- 이중 확장자: 파일명 내 `.` 구분 토큰이 (숨김파일 마커 제외) 2개 이상이면 거부하도록 구현했습니다.

### 확장자 없는 파일 / 점으로 시작하는 파일(.env) / 매우 긴 파일명
**구현완료**  
셋 중 하나라도 해당하면 1~4단계(콘텐츠 판별·이중확장자·위장·정책) 검사를 모두 통과한 뒤에만 파일명을 `{현재시간_초}.{실제확장자}`로 치환하여 저장했고, 이는 동일한 파일의 파일명을 바꾼다고 차단을 우회할 수 없도록 구현했습니다.

- **매우 긴 파일명 기준**: UTF-8 바이트 기준 255바이트 초과. 문자 수가 아니라 바이트 수를 기준으로 삼았습니다. 255바이트의 기준은 리눅스 파일시스템(`NAME_MAX`)이였습니다. 한글 등 멀티바이트 문자가 섞이면 문자 수 기준으로는 실제 저장 가능 여부를 보장할 수 없기 때문에 단위테스트로 문자 수와 바이트 수가 갈리는 케이스를 직접 검증했습니다.
- **점으로 시작하는 파일**: 앞의 점은 숨김파일 마커로 보고 확장자 구분자로 세지 않는다. `.env.local`처럼 내부에 점이 더 있어도 확장자를 "주장"하지 않는 것으로 처리했습니다.

### 확장자 입력값 검증
**구현완료**  
커스텀 확장자는 trim 후 영문/숫자만 허용(정규식 `^[A-Za-z0-9]+$`), 특수문자·공백·유니코드·`.` 포함은 전부 거부하고, 통과한 값만 소문자로 정규화해 저장하도록 구현했습니다.

### 서버 사이드 검증의 필요성
**구현완료**  
모든 최종 판단은 서버(`ExtensionPolicyService`, `FileUploadService`)에서 이루어집니다. 프론트의 `maxLength` 등은 UX 편의용일 뿐 실제 방어선이 아니며, curl로 우회 시도해도 서버에서 동일하게 막히도록 구현했습니다. 통합테스트로 검증을 완료하였습니다.

### 업로드 파일 크기 제한 / 개수 제한
**구현완료**  
- 크기: 10MB로 명시 설정. 업로드된 콘텐츠 전체를 메모리에 올려(`file.getBytes()`) 매직 넘버를 검사하는 구조라, 제한 없이 두면 한 요청이 힙을 과도하게 점유할 수 있어 10MB로 제한했습니다.
- 개수: 런타임 검사가 아니라 **API 시그니처 자체**로 1개로 고정했습니다.(`MultipartFile file` 단일 파라미터, `List` 아님) 다중 업로드는 과제 범위 밖으로 판단하였습니다.

### 저장 시 원본 파일명 사용 위험
**구현완료**  
물리 저장 파일명은 항상 `UUID.randomUUID()` 기반이며 사용자 입력이 파일시스템 경로 결정에 전혀 관여하지 않습니다.(경로 조작/디렉터리 traversal 원천 차단) 원본 파일명은 이력 테이블에 메타데이터로만 보관합니다. 저장 디렉터리도 Spring의 정적 리소스 서빙 경로 밖에 두고, 저장 후 POSIX 권한을 `rw-r-----`로 축소해 실행 비트를 제거했습니다. (1차 방어선(=접근할 URL이 없도록 함)이 깨지더라도 실행은 안 되도록 방어선을 구축)

### MIME 타입 스푸핑
**구현완료**  
클라이언트가 보낸 `Content-Type`은 애초에 코드 어디에서도 읽지 않고, 항상 매직 넘버를 통해 검증하도록 구현했습니다. 

---

## 정책/데이터 관점

### 고정 확장자와 커스텀 확장자가 겹칠 때
**구현완료**  
`extension_policy` 테이블 하나에 `type`(FIXED/CUSTOM) 컬럼으로 구분하고, `extension` 컬럼에 **타입 불문 대소문자 무시 유니크 인덱스**를 걸었습니다. 커스텀에 `exe`를 입력하면 이미 FIXED로 존재하는 값이라 애플리케이션 레벨에서 먼저 "고정 확장자에 있는 확장자입니다"라고 안내하고, 동시 요청으로 그 사전 체크를 우회하더라도 DB 유니크 제약이 최종 방어선 역할을 하도록 했습니다.

저는 여기서 고민을 했습니다. 간단히 생각하면 고정 확장자와 커스텀 확장자를 별개의 클래스로 구현하고, 추가로 이미 확장자가 존재하는지 판별하도록 할지, Enum의 type를 둠으로써 고정 확장자, 커스텀 확장자를 구분할지
저는 후자의 설계가 트레이드 오프 관계에서 이점이 더 많다고 판단하여 구현했습니다.
1. 테이블을 분리하지 않고, type을 통해 구분함으로서 수정보다는 확장에 열린 설계라고 생각했습니다.
2. 테이블을 분리했다면 서로 다른 두 테이블 사이의 값 충돌을 DB 유니크 제약으로 막을 방법이 없어 애플리케이션 코드에만 의존하게 될것이라고 판단했습니다.
3. 결과적으로 테이블을 분리하지 않고 설계함으로써 (중복 값 삽입으로 인한) 경쟁 상태를 방지할 수 있었습니다. 다만 이 유니크 인덱스는 값 충돌이 아니라 개수 초과 성격인 200개 상한 레이스는 막아주지 않는데, 그건 아래 "운영 관점"에서 다루는 것처럼 advisory lock으로 별도 방어했습니다.

### 정책 변경 이력/감사의 필요 여부
**부분적으로 구현, 전체 이력은 미구현.** 판단:
- **구현완료**
- 업로드 시도(성공/거부) 이력은 `upload_file` 테이블에 남겼습니다 — 운영 관점(로그/모니터링)과 직결되는, 비용 대비 가치가 명확한 로그라 판단했습니다.
- **구현미완료**
- "누가 언제 정책을 바꿨는지"에 대한 감사 로그(`extension_policy_audit` 같은 별도 테이블)는 만들지 않았습니다. 이유는 과제 범위상 관리자 인증 자체가 없어 관리주체가 없다고 판단했습니다. 대신 추후 필요하다면 `extension_policy` 변경 시점마다 `(extension, action, changed_at)`을 별도 테이블에 append하는 방식으로 확장 가능하다는 점 정도만 인지했습니다.

### 200개 / 20자 등 제한의 근거와 초과 시 UX
**구현완료**  
과제 스펙에 명시된 값(200개, 20자)을 그대로 채택했습니다. 초과 시:
- 20자 초과: 입력 자체를 프론트에서 `maxLength`로 막고, 서버에서도 재검증(`normalize()`)해 "영문/숫자만 입력할 수 있습니다"로 응답하도록 했습니다.
- 200개 초과: "최대 200개까지 등록할 수 있습니다" 메시지로 안내했습니다.

### 대량 확장자 조회/저장 시 성능·인덱스 고려
최대 207행(고정 7 + 커스텀 200) 규모라 지금 당장은 조회 성능 자체는 이슈가 되지 않는다고 판단했습니다.
나중에 "사용자별 정책" 같은 확장이 생겨 행 수가 커지면 그때는 `(user_id, extension)` 복합 인덱스가 실제 조회 성능을 개선할 수 있겠다라는 점 정도만 정리해놨습니다.

---

## UX/예외 관점

### 차단 시 사용자에게 보여줄 메시지
**구현완료**  
거부 사유별로 메시지를 구분했습니다.
이중 확장자("허용되지 않는 파일명입니다 (이중 확장자)"), 위장("파일의 실제 형식과 확장자가 일치하지 않습니다"), 정책 위반("차단된 확장자입니다: .exe") 등. 모든 4xx/5xx 응답이 `{ "message": "..." }` 형식으로 통일되어 있어(`GlobalExceptionHandler`) 프론트가 항상 같은 방식으로 메시지를 꺼내 보여줄 수 있도록 설계했습니다.

### 로딩/에러/네트워크 실패 상태 처리
**구현완료**  
각 컴포넌트가 loading/error 상태를 분리해서 관리합니다. 특히 "목록을 못 불러온 에러"(전체 화면 대체)와 "토글/추가/삭제 하나가 실패한 에러"(목록은 유지한 채 배너만 표시)를 구분했습니다. — 하나의 액션 실패로 전체 목록이 사라지는 건 나쁜 UX라고 판단했습니다.

### 저장 실패 시 화면 상태와 DB 상태의 일관성
**구현완료**  
체크박스 토글/삭제는 낙관적 업데이트(응답 기다리지 않고 즉시 반영) 후, 서버 요청이 실패하면 원래 상태로 롤백하도록 했습니다. 반면 커스텀 확장자 추가는 서버 응답을 받은 뒤에 목록에 반영하도록 했습니다 — 서버가 실제로 발급한 `id`가 필요해서 낙관적으로 먼저 보여줄 수 없기 때문입니다.

---

## 운영 관점

### 새로고침/동시 편집 시 정합성
**구현완료**  
정책 조회(`getCurrentlyBlockedExtensions()`)는 절대 캐시하지 않고 매 업로드 요청마다 DB에서 새로 읽도록 구현했습니다. — 캐시했다면 방금 막 차단된 확장자가 캐시 만료 전까지 계속 통과해버릴 수 있기 떄문입니다. 커스텀 확장자 동시 추가는 DB 유니크 제약으로 방어됩니다.

**200개 상한 체크의 경쟁 상태 방어**: 200개 상한 체크(`count >= 200`)는 값 충돌이 아니라 개수 초과이기떄문에, 앞서 다룬 유니크 인덱스로는 보호되지 않습니다. 
199개 상태에서 서로 다른 확장자를 동시에 추가하는 요청이 여러 개 들어오면 개수 확인(`countByType`)과 실제 insert 사이에 다른 요청이 끼어들어 201개 이상이 될 수 있다고 판단했습니다. 
이건 3장의 고려사항이 아니라 2번 최소 구현 범위에 명시된 "최대 200개까지 추가 가능"이라는 요구사항 자체가 깨지는 문제이기때문에, 검토 끝에 PostgreSQL이 제공하는 "advisory lock"(권고 락) 을 통해 해결하고 수정했습니다.

`SELECT ... FOR UPDATE`는 잠글 대상 행이 마땅치 않아(카운트는 집계값이지 특정 행이 아님) 대신 PostgreSQL의 트랜잭션 범위 advisory lock(`pg_advisory_xact_lock`)을 선택했습니다. 
addCustomExtension() 시작 부분에서 고정 키로 락을 걸어, 개수 확인 후 insert하는 구간을 사실상 하나의 원자적 구간으로 만들고 트랜잭션이 끝나면 자동으로 락이 풀리도록 구현했습니다. 실제 PostgreSQL에 붙여 199개 상태에서 5개 요청을 동시에 발사하는 통합테스트(`ExtensionPolicyServiceConcurrencyTest`)로 정확히 1개만 성공하고 최종 200개로 고정되는 것까지 확인했습니다.

### 로그/모니터링 관점에서 무엇을 남길 것인가
**구현완료, 두 가지를 병행.**
- SLF4J 로그는 업로드 성공/거부 시점마다 `original`, `realExtension`, `reason`을 남깁니다. 실시간 운영 디버깅용과 같습니다.
- upload_file 테이블은 같은 정보를 DB에도 남깁니다. 로그와 목적이 다르다 — 로그는 검색/집계가 번거롭고 배포 환경에 따라 보존 기간이 짧을 수 있는 반면, 테이블은 어떤 확장자가 자주 차단됐는지류의 질문에 SQL로 바로 답할 수 있기 때문에 이렇게 구현하였습니다.

### 향후 확장 고려 여부
**문서화만 함(미구현)**
- 사용자별 정책: 지금 스키마에 `user_id` 컬럼과 `(user_id, extension)` 복합 유니크 인덱스를 추가하면 확장 가능한 구조입니다.
- 화이트리스트 방식 전환은 현재 블랙리스트 모델을 화이트리스트로 바꾸려면 `FileSignatureDetector`의 미지 바이너리는 통과 정책을 반대로 뒤집어야 하는데, 이는 아키텍처 성격 자체가 바뀌는 결정이라 추후에 별도 논의가 필요하다고 판단했습니다.

---

## 스스로 찾아낸 고려사항

과제 문서에 명시되지 않았지만 구현 과정에서 직접 발견하고 대응한 것들입니다!

### Null byte injection과 로그 저장 시 이스케이프
파일명에 null byte(`\0`)가 섞인 경우 즉시 거부하도록 방어했습니다. 
추가적으로 원본 파일명을 그대로 이력 테이블에 저장하면 PostgreSQL의 text 계열 컬럼이 null byte를 포함한 문자열 자체를 거부해 INSERT가 실패합니다.
따라서 `\0`을 `\\0`으로 이스케이프한 뒤 저장하도록 구현습니다.

### RTLO — 유니코드 방향 제어 문자를 이용한 파일명 시각적 위장
파일명에 RTLO(U+202E) 등 유니코드 방향 제어 문자가 섞이면, 실제 확장자는 .exe인데 화면엔 .doc처럼 보이게 시각적으로 위장할 수 있습니다.
매직 넘버 기반 차단 자체는 우회되지 않지만, 원본 파일명을 그대로 노출하면 사용자가 속을 수 있어 해당 제어 문자(U+202A-E, U+2066-9) 포함 시 즉시 거부하도록 구현했습니다.
거부 사유를 로그/이력에 남길 때도 원본 그대로 두면 로그 줄 자체가 뒤집혀 보일 수 있어, null byte처럼 `[U+202E]` 표기로 치환한 뒤 저장하도록 했습니다.

### PE / 텍스트 / ZIP 계열의 매직 넘버 충돌
`exe/com/scr/cpl`이 동일 헤더를 쓰고, `js/bat/cmd`류는 매직 넘버가 아예 없으며, `docx/xlsx` 등은 zip과 시그니처가 같다는 사실은 정확히 알지 못했습니다. 
"계열 판정 후 claimed가 계열 내 멤버면 인정, 밖이면 대표값으로 위장 검출"이라는 규칙을 Claude와 함께 설계했습니다.

### HTTP 상태 코드의 정밀한 구분
에러 상황마다 상태 코드를 구분했습니다. 
정책 리소스 중복은 409(Conflict), Bean Validation 실패나 잘못된 JSON은 400(Bad Request), 존재하지 않는 확장자 대상 요청은 404(Not Found), 업로드 파일이 검증 규칙을 위반한 경우는 422(Unprocessable Content, "요청 구문은 정상이지만 내용이 처리 불가"), 파일 크기 초과는 413(Content Too Large). 이 구분이 없으면 클라이언트가 "내 잘못인지 서버 잘못인지"를 상태 코드만으로 판단할 수 없다.

### 타임스탬프에 Instant를 쓰는 이유
로컬 개발(KST)과 배포 서버(보통 UTC)가 같은 테이블에 값을 쓰면 같은 순간지만 저장된 값이 달라진는 LocalDateTime대신 Instant로 통일하였습니다.

### 배포 환경에서 로컬 디스크 저장의 영속성 트레이드오프
재배포 시 컨테이너에 저장된 파일이 전부 사라진다는 것을 알고 있었지만, 이번 과제에서는 없어져도 무방하다고 판단하여 S3/GCS 같은 오브젝트 스토리지를 별도로 도입하지 않았습니다. 실서비스로 전환한다면 반드시 교체가 필요한 지점입니다.

### 이력 테이블 컬럼 길이와 "안 잘리게" 방어
upload_file.detected_extension은 VARCHAR(20)인데, FileSignatureDetector가 미지의 바이너리에 대해 파일명이 주장한 확장자를 길이 제한 없이 그대로 돌려주는 경로가 있었습니다.
이러한 문제는 INSERT시 정상 업로드가 되도 500 에러로 응답하는 버그를 발생시켰습니다. 
따라서 확장자 자체의 최대 길이(`CUSTOM_EXTENSION_MAX_LENGTH`, 20자)로 일관되게 맞춰 그보다 긴 끝 토큰은 애초에 확장자로 인정하지 않도록(=확장자 없음과 동일하게 특수 파일명 치환 대상으로) 수정하여 해결했습니다.

같은 이유로 `original_filename`(`VARCHAR(500)`)도 점검했고,
정상 파일명의 보통 최대는 255바이트(`NAME_MAX`)지만, null byte 이스케이프하는(`\0`→`\\0`) 과정에서 길이가 늘어날 수 있어 여유분까지 포함해 500바이트로 잡았습니다. 
그래도 조작된 요청 등으로 넘는 값이 들어올 가능성이 있어, `UploadFile` 생성자에서 저장 직전 길이를 잘라내는 방어도 추가했습니다.

### 매직 넘버 커버리지의 한계 — 다운로드 기능이 없어 실질적 위험은 없음
.msi같이 FileSignatureDetector가 모르는 위험한 포맷도 있다는 것을 인지하고 있습니다. 
다만 업로드된 파일을 조회·다운로드하는 API 자체가 없어서, 어떤 확장자로 기록되든 실행할 방법이 없어 참고 수준으로만 CONSIDERATION.md에 남깁니다.

### 파일 파트 누락이 500으로 응답되던 버그
file 파트 없이(또는 Content-Type이 multipart가 아닌 채로) 업로드 요청을 보내면 500이 응답됐습니다.
MultipartException, MissingServletRequestPartException 둘 다 전용 핸들러가 없어 catch-all(500)로 떨어진 게 원인이었고, 전용 핸들러를 추가해 400으로 고쳤습니다.


---

## Out of Scope로 명시한 것

- 관리자 로그인/인증 체계 : 현재 인증 없이 접근 가능한 상태입니다. 과제 범위 밖으로 판단했습니다.
- 정책 변경 감사 로그 : 관리자 인증 자체가 없어 "누가" 바꿨는지 특정할 관리 주체가 없고, 우선순위도 낮다고 판단해 구현하지 않았습니다.
- 바이러스/악성코드 스캔 연동 : 매직 넘버 기반 판별은 파일 형식을 검증하는 것이지 안전성을 보장하지 않습니다. 과제 구현 범위를 벗어난다고 판단했습니다.
- 업로드된 파일의 목록 조회/다운로드/미리보기 UI : 과제 요구사항에 없어 구현하지 않았고, upload_file 테이블은 순수 쓰기 전용 이력으로만 쓸 수 있도록 구현했습니다.
- 사용자별 정책, 화이트리스트 전환 : 문서화만 하고 구현하지 않았습니다. 사용자별 정책은 user_id 컬럼과 복합 인덱스 추가로, 화이트리스트 전환은 미지 바이너리 판정 정책을 뒤집는 것으로 확장 가능합니다.
