# 고려사항 문서

과제 원문(`과제_파일업로드_AI개발.md`) 3장의 항목 순서를 그대로 따르되, 각 항목마다 **구현 여부와 판단 근거**를 남긴다. 마지막 절은 과제 문서에 없던, 구현 과정에서 직접 발견한 고려사항이다.

---

## 3-1. 검증/보안 관점

### 확장자만 믿어도 되는가? (매직 넘버 검증)
**구현함.** `FileSignatureDetector`가 업로드된 파일의 **콘텐츠 바이트**만으로 실제 형식을 판별한다. 클라이언트가 보낸 `Content-Type`(MIME)은 어디에서도 참조하지 않는다.

문제는 매직 넘버만으로 확장자 하나를 정확히 특정할 수 없는 경우다:
- `exe/com/scr/cpl`은 전부 동일한 Windows PE(`MZ`) 헤더를 쓴다 — 콘텐츠만으론 서로 구분 불가능
- `js/bat/cmd` 등 텍스트 스크립트는 매직 넘버 자체가 없다
- `docx/xlsx/pptx`는 내부적으로 ZIP 컨테이너라 순수 zip과 시그니처가 같다

**판단**: 이 세 "계열"에 대해서는 콘텐츠가 그 계열에 속함을 확인한 뒤, 파일명이 주장하는 확장자가 계열의 합리적 멤버면 인정하고 계열 밖이면 대표값(PE→`exe`, 텍스트→`txt`, zip→`zip`)으로 판정해 위장 검사에서 걸리게 했다. 과제가 예시로 든 "`report.jpg`인데 실제로는 실행 파일"이 정확히 이 로직(PE인데 claimed가 PE계열 밖)으로 잡힌다 — 통합테스트로 검증됨.

계열에도, 개별 시그니처(PNG/JPG/PDF 등)에도 해당하지 않는 **완전히 미지의 바이너리**는 claimed를 그대로 인정하도록 했다. 그렇지 않으면 시그니처를 등록해두지 않은 모든 정상 포맷이 "위장"으로 오판되어 차단되는데, 이는 과제가 요구한 "확장자 블랙리스트" 모델을 "우리가 아는 포맷만 허용하는 화이트리스트" 모델로 바꿔버리는 과도한 차단이라고 판단했다.

### 대소문자 / 이중 확장자
**구현함.**
- 대소문자: 확장자는 저장/비교 시 항상 소문자로 정규화한다(`normalize()`, `FilenameAnalyzer`).
- 이중 확장자: 파일명 내 `.` 구분 토큰이 (숨김파일 마커 제외) 2개 이상이면 거부한다(`invoice.pdf.exe` 등).

### 확장자 없는 파일 / 점으로 시작하는 파일(.env) / 매우 긴 파일명
**구현함.** 셋 중 하나라도 해당하면 1~4단계(콘텐츠 판별·이중확장자·위장·정책) 검사를 **모두 통과한 뒤에만** 파일명을 `{현재시간_초}.{실제확장자}`로 치환한다. 즉 이름을 바꾼다고 차단을 우회할 수 없다.

- **매우 긴 파일명 기준**: UTF-8 바이트 기준 255바이트 초과. 문자 수가 아니라 바이트 수로 잰다 — 대부분의 리눅스 파일시스템(`NAME_MAX`)이 바이트 단위로 파일명 길이를 제한하고, 한글 등 멀티바이트 문자가 섞이면 문자 수 기준으로는 실제 저장 가능 여부를 보장할 수 없기 때문이다. 단위테스트로 문자 수와 바이트 수가 갈리는 케이스를 직접 검증했다.
- **점으로 시작하는 파일**: 앞의 점은 숨김파일 마커로 보고 확장자 구분자로 세지 않는다. `.env.local`처럼 내부에 점이 더 있어도 확장자를 "주장"하지 않는 것으로 처리했다 — 자세한 배경은 아래 "이전 기수 코드에서 발견한 버그" 참고.

### 확장자 입력값 검증
**구현함.** 커스텀 확장자는 trim 후 영문/숫자만 허용(정규식 `^[A-Za-z0-9]+$`), 특수문자·공백·유니코드·`.` 포함은 전부 거부하고, 통과한 값만 소문자로 정규화해 저장한다.

### 서버 사이드 검증의 필요성
**구현함.** 모든 최종 판단은 서버(`ExtensionPolicyService`, `FileUploadService`)에서 이루어진다. 프론트의 `maxLength` 등은 UX 편의용일 뿐 실제 방어선이 아니며, curl로 우회 시도해도 서버에서 동일하게 막힌다(통합테스트로 검증).

### 업로드 파일 크기 제한 / 개수 제한
**구현함.**
- 크기: 10MB로 명시 설정. 업로드된 콘텐츠 전체를 메모리에 올려(`file.getBytes()`) 매직 넘버를 검사하는 구조라, 제한 없이 두면 한 요청이 힙을 과도하게 점유할 수 있어 근거를 두고 값을 정했다.
- 개수: 런타임 검사가 아니라 **API 시그니처 자체**로 1개로 고정했다(`MultipartFile file` 단일 파라미터, `List` 아님). 다중 업로드는 과제 범위 밖으로 판단.

### 저장 시 원본 파일명 사용 위험
**구현함.** 물리 저장 파일명은 항상 `UUID.randomUUID()` 기반이며 사용자 입력이 파일시스템 경로 결정에 전혀 관여하지 않는다(경로 조작/디렉터리 traversal 원천 차단). 원본 파일명은 이력 테이블에 메타데이터로만 보관한다. 저장 디렉터리도 Spring의 정적 리소스 서빙 경로 밖에 두고, 저장 후 POSIX 권한을 `rw-r-----`로 축소해 실행 비트를 제거한다(1차 방어선이 깨지더라도 실행은 안 되도록 방어를 한 겹 더 둠).

### MIME 타입 스푸핑
**구현함.** 클라이언트가 보낸 `Content-Type`은 애초에 코드 어디에서도 읽지 않는다. 매직 넘버 검증이 이 문제를 구조적으로 해결한다.

---

## 3-2. 정책/데이터 관점

### 고정 확장자와 커스텀 확장자가 겹칠 때
**구현함.** `extension_policy` 테이블 하나에 `type`(FIXED/CUSTOM) 컬럼으로 구분하고, `extension` 컬럼에 **타입 불문 대소문자 무시 유니크 인덱스**를 걸었다. 커스텀에 `exe`를 입력하면 이미 FIXED로 존재하는 값이라 애플리케이션 레벨에서 먼저 "고정 확장자에 있는 확장자입니다"로 안내하고, 동시 요청으로 그 사전 체크를 우회하더라도 DB 유니크 제약이 최종 방어선 역할을 한다.

테이블을 분리하지 않은 이유: 분리했다면 서로 다른 두 테이블 사이의 값 충돌을 DB 유니크 제약으로 막을 방법이 없어 애플리케이션 코드에만 의존하게 되고, 이는 곧 진짜 경쟁 상태(race condition)로 이어진다.

### 정책 변경 이력/감사의 필요 여부
**부분적으로 구현, 전체 이력은 미구현.** 판단:
- **구현함**: 업로드 시도(성공/거부) 이력은 `upload_file` 테이블에 남긴다 — 3-4 운영 관점과 직결되는, 비용 대비 가치가 명확한 로그라 판단했다.
- **미구현**: "누가 언제 정책을 바꿨는지"에 대한 감사 로그(`extension_policy_audit` 같은 별도 테이블)는 만들지 않았다. 과제 범위상 관리자 인증 자체가 없어(아래 참고) "누가"를 특정할 방법이 없고, 3일 데드라인 안에서 우선순위가 핵심 파이프라인보다 낮다고 판단했다. 필요하다면 `extension_policy` 변경 시점마다 `(extension, action, changed_at)`을 별도 테이블에 append하는 방식으로 확장 가능하다.

### 200개 / 20자 등 제한의 근거와 초과 시 UX
과제 스펙에 명시된 값(200개, 20자)을 그대로 채택했다. 초과 시:
- 20자 초과: 입력 자체를 프론트에서 `maxLength`로 막고, 서버에서도 재검증(`normalize()`)해 "영문/숫자만 입력할 수 있습니다"로 응답
- 200개 초과: "최대 200개까지 등록할 수 있습니다" 메시지로 명확히 안내

### 대량 확장자 조회/저장 시 성능·인덱스 고려
최대 207행(고정 7 + 커스텀 200) 규모라 조회 성능 자체는 이슈가 되지 않는다고 판단했다. 유니크 인덱스는 성능보다 **정합성 제약**이 주목적이다. 이 점을 CONSIDERATIONS에 명시하는 이유는, 나중에 "사용자별 정책" 같은 확장이 생겨 행 수가 커지면(예: 사용자 수 × 확장자 수) 그때는 `(user_id, extension)` 복합 인덱스가 실제 조회 성능에 기여하게 될 것이라는 점도 함께 인지하고 있기 때문이다.

---

## 3-3. UX/예외 관점

### 차단 시 사용자에게 보여줄 메시지
**구현함.** 거부 사유별로 메시지를 구분했다: 이중 확장자("허용되지 않는 파일명입니다 (이중 확장자)"), 위장("파일의 실제 형식과 확장자가 일치하지 않습니다"), 정책 위반("차단된 확장자입니다: .exe") 등. 모든 4xx/5xx 응답이 `{ "message": "..." }` 형식으로 통일되어 있어(`GlobalExceptionHandler`) 프론트가 항상 같은 방식으로 메시지를 꺼내 보여줄 수 있다.

### 로딩/에러/네트워크 실패 상태 처리
**구현함.** 각 컴포넌트가 loading/error 상태를 분리해서 관리한다. 특히 "목록을 못 불러온 에러"(전체 화면 대체)와 "토글/추가/삭제 하나가 실패한 에러"(목록은 유지한 채 배너만 표시)를 구분했다 — 하나의 액션 실패로 전체 목록이 사라지는 건 나쁜 UX라고 판단했다.

### 저장 실패 시 화면 상태와 DB 상태의 일관성
**구현함.** 체크박스 토글/삭제는 낙관적 업데이트(응답 기다리지 않고 즉시 반영) 후, 서버 요청이 실패하면 원래 상태로 롤백한다. 반면 커스텀 확장자 추가는 서버 응답을 받은 뒤에 목록에 반영한다 — 서버가 실제로 발급한 `id`가 필요해서 낙관적으로 먼저 보여줄 수 없기 때문이다.

### 접근성/반응형
**부분 구현.** 체크박스는 `<label>`로 텍스트와 연결했고(스위치 UI로 바꾼 뒤에도 시맨틱은 checkbox 유지), 삭제 버튼엔 `aria-label`, 에러/성공 메시지엔 `role="alert"`/`role="status"`를 부여했다. 다만 전체 반응형 미디어쿼리, 키보드 전용 사용성에 대한 정밀한 QA는 하지 않았다 — 평가 비중이 낮은 프론트 영역이라 시간을 깊게 투자하지 않기로 판단했다.

---

## 3-4. 운영 관점

### 새로고침/동시 편집 시 정합성
**구현함.** 정책 조회(`getCurrentlyBlockedExtensions()`)는 절대 캐시하지 않고 매 업로드 요청마다 DB에서 새로 읽는다 — 캐시했다면 방금 막 차단된 확장자가 캐시 만료 전까지 계속 통과해버릴 수 있다. 커스텀 확장자 동시 추가는 DB 유니크 제약으로 방어한다.

**알려진 잔여 레이스**: 200개 상한 체크(`count >= 200`)는 유니크 인덱스로 보호되지 않는다. 199개 상태에서 서로 다른 확장자를 동시에 추가하는 두 요청이 있으면 201개가 될 수 있다. 값 충돌이 아니라 "개수 초과"라 유니크 인덱스로 못 잡고, `SELECT ... FOR UPDATE` 같은 락이 필요한데 200개라는 상한이 정확히 지켜야 하는 엄격한 값이 아니라고 판단해 의도적으로 손대지 않았다.

### 로그/모니터링 관점에서 무엇을 남길 것인가
**구현함, 두 가지를 병행.**
- SLF4J 로그: 업로드 성공/거부 시점마다 `original`, `realExtension`, `reason`을 남긴다. 실시간 운영 디버깅용.
- `upload_file` 테이블: 같은 정보를 DB에도 남긴다. 로그와 목적이 다르다 — 로그는 검색/집계가 번거롭고 배포 환경에 따라 보존 기간이 짧을 수 있는 반면, 테이블은 "최근에 어떤 확장자가 자주 차단됐는지"류의 질문에 SQL로 바로 답할 수 있다.

### 향후 확장 고려 여부
문서화만 함(미구현):
- 사용자별 정책: 지금 스키마에 `user_id` 컬럼과 `(user_id, extension)` 복합 유니크 인덱스를 추가하면 확장 가능한 구조.
- 화이트리스트 방식 전환: 현재 블랙리스트 모델을 화이트리스트로 바꾸려면 `FileSignatureDetector`의 "미지 바이너리는 통과" 정책을 반대로(미지 바이너리는 차단) 뒤집어야 하는데, 이는 아키텍처 성격 자체가 바뀌는 결정이라 별도 논의가 필요하다고 판단.

---

## 여기 없는데 스스로 찾아낸 고려사항

과제 문서에 명시되지 않았지만 구현 과정에서 직접 발견하고 대응한 것들이다.

### 1. PE / 텍스트 / ZIP 계열의 매직 넘버 충돌
`exe/com/scr/cpl`이 동일 헤더를 쓰고, `js/bat/cmd`류는 매직 넘버가 아예 없으며, `docx/xlsx` 등은 zip과 시그니처가 같다는 사실은 과제 어디에도 언급이 없다. "계열 판정 후 claimed가 계열 내 멤버면 인정, 밖이면 대표값으로 위장 검출"이라는 규칙을 직접 설계했다.

### 2. 이전 기수 제출 코드의 `.env.local` 버그
참고용으로 검토한 이전 기수 코드는 `.env.local`처럼 점으로 시작하면서 내부에 점이 더 있는 파일명에서, 내부 점 뒤의 문자열(`local`)을 확장자로 오인했다. 이 경우 3단계 위장 검사가 실행되어, 실제로는 문제없는 파일이 "확장자 위장"으로 부당하게 거부되는 결함이 있었다. 원인을 분석해 "점으로 시작하는 이름은 확장자를 아예 주장하지 않는다"로 규칙을 수정하고, 재발 방지 테스트를 추가했다.

### 3. Null byte injection과 로그 저장 시 이스케이프
파일명에 null byte(`\0`)가 섞인 경우(`shell.php\0.jpg`류의 고전적 업로드 우회 기법) 즉시 거부하도록 방어했다. 여기서 한 가지 더: 원본 파일명을 그대로 이력 테이블에 저장하면 PostgreSQL의 text 계열 컬럼이 null byte를 포함한 문자열 자체를 거부해 INSERT가 실패한다 — "보안 로그를 남기려던 코드가 그 자체로 예외를 던지는" 상황이 생길 뻔했다. `\0`을 `\\0`으로 이스케이프한 뒤 저장하도록 고쳤다.

### 4. HTTP 상태 코드의 정밀한 구분
에러 상황마다 상태 코드를 구분했다: 정책 리소스 중복은 409(Conflict), Bean Validation 실패나 잘못된 JSON은 400(Bad Request), 업로드 파일이 검증 규칙을 위반한 경우는 422(Unprocessable Entity, "요청 구문은 정상이지만 내용이 처리 불가"), 파일 크기 초과는 413(Content Too Large). 이 구분이 없으면 클라이언트가 "내 잘못인지 서버 잘못인지"를 상태 코드만으로 판단할 수 없다.

### 5. `boolean` vs `Boolean` — 필드 누락의 침묵 실패
고정 확장자 토글 API(`PATCH .../fixed/{extension}`)의 요청 바디를 원시 타입 `boolean`으로 받으면, 클라이언트가 실수로 `blocked` 필드를 빠뜨렸을 때 Jackson이 조용히 `false`로 역직렬화해버린다. 즉 "차단 해제"를 요청한 적이 없는데도 필드 누락만으로 차단이 풀리는, 보안 정책 API에서 특히 위험한 실패다. 박싱 타입 `Boolean` + `@NotNull`로 바꿔 필드 누락이 명시적인 400 에러가 되도록 했다.

### 6. 프론트/백엔드 분리 배포와 CORS
프론트(Vercel)와 백엔드(Render)를 별도 오리진으로 배포하기로 하면서, 로컬 개발 단계에서 실제로 브라우저로 열어봤을 때 CORS 에러를 직접 발견했다. 전체 허용(`*`) 대신 허용 오리진을 환경변수로 명시 지정하는 방식으로 설정했다 — 파일 업로드를 다루는 API라 임의 사이트의 요청을 열어두는 것 자체가 불필요한 공격 표면이라 판단했다.

### 7. 타임스탬프에 `Instant`를 쓰는 이유
`LocalDateTime`은 타임존 정보가 없는 "벽시계 시각"이다. 로컬 개발(KST)과 배포 서버(보통 UTC)가 같은 테이블에 값을 쓰면 같은 순간인데 저장된 값이 달라진다. `Instant`로 통일하고 DB 컬럼도 `TIMESTAMPTZ`로 맞췄다.

### 8. 배포 환경에서 로컬 디스크 저장의 영속성 트레이드오프
Render 컨테이너의 로컬 디스크는 컨테이너 생명주기와 함께 간다 — 재배포·재시작 시 그 안에 저장된 파일은 전부 사라진다. Persistent Disk 같은 유료 애드온 없이 이 상태로 배포하기로 판단했다: 이 과제의 목적은 "면접 당일 그 자리에서 데모"이고, 컨테이너가 살아있는 한 세션 안에서는 업로드가 정상적으로 유지·조회된다. 오브젝트 스토리지(S3 등) 도입은 3일이라는 시간 대비 실익이 낮다고 봐서 의도적으로 제외했다. 실서비스로 전환한다면 S3/GCS 같은 오브젝트 스토리지로 교체가 반드시 필요한 지점이다.

---

## Out of Scope로 명시한 것

- 관리자 로그인/인증 체계: `/api/extensions/*`는 인증 없이 접근 가능한 상태. 과제 범위 밖으로 판단했으나, 실제 서비스라면 최우선으로 채워야 할 구멍이라는 점을 인지하고 있다.
- 정책 변경 감사 로그: 위 3-2 참고.
- 바이러스/악성코드 스캔 연동: 매직 넘버 기반 판별은 파일 "형식"을 검증하는 것이지 "안전성"을 보장하지 않는다. 예를 들어 매크로 포함 docx는 이 시스템을 그대로 통과한다. 별도 스캔 엔진 연동이 필요한 영역이며, 3일 범위를 벗어난다고 판단했다.
- 업로드된 파일의 목록 조회/다운로드/미리보기 UI: 과제 요구사항에 없어 구현하지 않았고, `upload_file` 테이블은 순수 쓰기 전용 이력으로만 쓰인다.
- 사용자별 정책, 화이트리스트 전환: 위 3-4 "향후 확장" 참고.
