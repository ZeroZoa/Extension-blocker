# AI 활용 기록

이 과제 전체를 Claude Code(AI 코딩 에이전트)와의 대화로 진행했다. 아래는 그 과정을 시간 순으로 재구성한 기록이다 — 프롬프트를 그대로 나열하기보다, **어떤 의도로 물었는지 / 결과가 마음에 안 들 때 어떻게 다시 물었는지 / AI 결과를 그대로 썼는지, 고쳤는지, 버렸는지**를 중심으로 정리했다.

---

## 1. 기획 단계 (스택·아키텍처·배포 전략)

**의도**: 회사 노트북이라 개발 환경이 없는 상태에서, 실제 코딩 전에 스택/아키텍처/배포 전략부터 정하고 싶었다. AI에게 "면접관 입장에서" 판단해달라고 역할을 부여해 평가자 관점의 조언을 끌어냈다.

- 프론트엔드를 Flutter로 할지 React로 할지 물었고, "웹서버 개발자 채용", "접근성 요구사항" 두 근거로 React를 추천받아 **그대로 채택**했다.
- 배포 전략에서 처음엔 Railway+Vercel+S3 조합을 제안받았는데, "파일 스토리지는 필요 없다", "콜드스타트 없는 걸로", "렌더는 어때?" 세 번에 걸쳐 요구사항을 좁혀가며 최종적으로 **Render(유료, 백엔드+DB 통합) + Vercel(프론트)**로 수렴시켰다. S3 제안은 **버렸다** — 데모 목적상 파일 영속성이 필요 없다고 판단했기 때문.

## 2. 참고 프로젝트 검토 및 방향 전환

이전 기수 지원자가 제출한 코드(Spring Boot + H2 + Thymeleaf)를 첨부해 "흐름만 참고하고, 로직은 전면 재검증"하라고 지시했다. AI가 처음엔 이 코드를 "리팩토링"하는 쪽으로 접근하려 했으나, 이게 **다른 지원자의 제출물**이라는 걸 명확히 하고 "그 코드는 무시해, 흐름만 잡고 완전히 새로 짜"라고 정정했다. 이 지시 이후 실제로 `FilenameAnalyzer` 재설계 과정에서 그 코드의 버그(`.env.local`을 확장자 있는 파일로 오인)를 AI가 스스로 찾아냈다 — 그대로 베꼈다면 못 잡았을 결함이다.

## 3. 프로젝트 셋업

Spring Initializr를 curl로 자동 생성하는 안을 AI가 먼저 제안했는데, "start.io로 할게"라며 **웹 UI로 직접 하겠다고 거부**했다. 이후 Maven이 아니라 Gradle로 바꿔달라고 요청 — AI는 별 이견 없이 그대로 반영. 이 과정에서 "명령어는 넘겨주고 내가 직접 실행하고 싶다"는 작업 방식을 요청했고, 이후 전체 세션에서 이 원칙을 유지했다(단, git init 등 일부는 AI가 실수로 대신 실행해 스스로 정정하기도 했다).

## 4. `extension` 도메인 — 1차 구현과 되돌림

AI가 처음에 Flyway 마이그레이션 + `schema.sql` 방식을 짰는데, "코드만 엄격한 실무기준으로, 환경은 정확하고 빠른 배포가 우선이니 Flyway 필요 없다"고 정정했다. **AI 결과를 고쳐 쓴 사례**: Flyway 의존성/마이그레이션 파일을 전부 제거하고 `schema.sql`+`data.sql` 방식으로 되돌렸다.

## 5. "네가 다 알아서 짜면 어떡하냐" — 협업 방식 전환

AI가 여러 클래스를 한 번에 완성해서 보여주자 "코드를 이해해야 면접에서 대답하는데, 네가 마음대로 작성해버리면 어떡하냐"고 제동을 걸었다. 이 피드백 이후 AI는 매 구현 전에 설계를 먼저 설명하고, 구현 후엔 핵심 판단 지점을 짚어 이해도를 확인하는 방식으로 전환했다(예: `@MappedSuperclass` 존재 이유, DB 유니크 제약과 동시성 방어의 관계, 422 vs 400 상태 코드 차이 등을 직접 설명해보라고 요구받았고, 매번 실제로 답변).

## 6. 엔티티 설계 지적 — AI가 놓친 것을 사용자가 잡아낸 사례

`ExtensionPolicy` 엔티티를 처음 짤 때 AI는 `LocalDateTime` + 개별 getter/setter로 작성했다. 여기서 **"BaseTimeEntity로 하고 LocalDateTime 쓰지 말고 Instant 써야지"**라고 직접 지적했다 — 로컬(KST)과 배포 서버(UTC)가 같은 테이블에 쓸 때 타임존 불일치가 생길 수 있다는 이유였다. AI가 놓쳤던 부분을 사용자가 먼저 찾아낸 대표 사례라 특히 강조한다. 이후 Lombok 도입도 직접 요청했고, AI는 `@Data`를 쓰지 않고 `@Getter`+필드 단위 `@Setter`로 제한한 이유를 설명한 뒤 반영했다.

## 7. Bean Validation — "추가가 뭐야, 전면 리팩토링해"

AI가 DTO에 `@NotBlank`만 최소로 추가하자고 제안했을 때 "전면 리팩토링해 개선할 수 있으면"이라고 요구 수준을 올렸다. 이 과정에서 AI가 스스로 발견해 제안한 것:
- `boolean` → `Boolean` + `@NotNull` 전환(필드 누락 시 침묵 실패 방지) — **그대로 채택**
- DTO 레벨 `@Pattern`/`@Size`는 trim 순서 문제(원본 문자열에 대해 실행되어 trim하면 통과할 입력을 조기 거부) 때문에 **의도적으로 추가하지 않음** — AI가 자체적으로 트레이드오프를 판단해 제안했고, 그대로 채택.

## 8. `upload` 도메인 — 설계 논의 중 발견한 버그

`FileSignatureDetector` 설계를 논의하는 과정에서 "판단은 과제 파일 기준으로 해달라"고 요구해, PE 계열 충돌 같은 디테일이 과제에 없는 내용이라는 걸 AI가 명시적으로 확인하게 했다. 여기서 AI가 제안한 "미지 바이너리는 `bin`으로 판정해 항상 위장 취급"안이 **과도한 차단**이라는 걸 사용자가 지적하지 않고 AI 스스로 재검토해 "claimed를 그대로 인정" 안으로 **고쳤다**(사용자는 이 수정 방향에 동의만 함).

## 9. 스코프 조절 — "AI 의존 티 안 나게"

"모든 걸 체크하고 제출하면 AI에 의존한 티가 난다"며, 구현 범위를 어디서 끊을지 먼저 논의했다. AI가 제시한 "필수 파이프라인 + 킥 2~3개(PE 충돌, null byte, 동시성)만 완성도 있게, 나머지는 문서화"라는 기준에 동의했고, 실제로 감사 로그·화이트리스트 전환·관리자 인증은 구현하지 않고 문서로만 남겼다.

## 10. 테스트 작성

"핵심 시나리오만" 짜라고 미리 범위를 제한했다. AI가 `FilenameAnalyzer`/`FileSignatureDetector`/`FileUploadService` 세 클래스에 총 22개 테스트를 작성했고, 전부 로컬에서 통과를 직접 확인했다(Docker Postgres 포트 충돌 문제를 겪으며 5434번으로 우회).

## 11. 프론트엔드 — 실제 브라우저 검증에서 CORS 버그 발견

프론트를 다 만든 뒤 "브라우저에서 실제로 확인해달라"고 요구하지 않았는데도 AI가 자체적으로 Browser 도구로 열어봤고, 그 과정에서 **CORS 미설정**을 실제로 발견했다(콘솔 에러 확인). 이건 코드 리뷰만으로는 못 잡고 실제로 띄워봐야 나오는 종류의 버그였다. `WebConfig`를 추가해 허용 오리진을 명시하는 방식으로 고쳤다.

## 12. 화면 구조 단순화

AI가 처음엔 두 화면(정책관리/업로드)을 라우터나 탭으로 분리하자고 제안했는데, "보통 한 페이지 안에 전부 있던데"라고 반박했다. 과제 원본 이미지도 한 화면만 보여준다는 걸 재확인한 뒤, 라우팅 없이 한 페이지에 배치하는 쪽으로 **AI 제안을 버리고 사용자 의견을 채택**했다 — 의존성도 줄고 원본 요구사항에도 더 맞는 방향이었다.

## 13. 디자인 — 구체적 레퍼런스 제시

"버튼은 버튼처럼", "아이폰 스위치처럼", "메인컬러는 #ac00ca", 이후 "토스처럼 다듬어달라"는 식으로 구체적인 톤을 계속 제시했다. AI는 iOS 스타일 토글 스위치, 카드+그림자 기반 레이아웃, `::file-selector-button`을 이용한 네이티브 파일 입력 커스터마이징으로 대응했고, 매번 실제 브라우저 스크린샷으로 확인 후 다음 단계로 진행했다.

## 14. 배포 준비

Dockerfile이 없다는 걸 AI가 먼저 짚었고, 멀티스테이지 빌드로 작성한 뒤 로컬에서 실제로 `docker build` + 컨테이너 기동 + API 응답까지 직접 검증했다(문서에만 "될 것이다"라고 적지 않고 실제로 띄워봄). 이 과정에서 `uploads-data/`가 `.gitignore`에 빠져 있어 로컬 테스트 파일이 커밋될 뻔한 것도 미리 발견해 고쳤다.

---

## 사용한 스킬/도구/MCP

| 도구 | 용도 |
|---|---|
| Claude Code (Sonnet 5) | 전체 개발 세션 — 설계 논의, 코드 작성, 리뷰 |
| JetBrains MCP | IDE에 열린 프로젝트 상태(모듈, git 저장소, 디렉터리 구조) 확인 — 두 프로젝트(백엔드/프론트)가 동시에 열려 있을 때 프로젝트 경로 명시에 사용 |
| Claude Browser (Chrome 기반 자동화) | 프론트엔드를 실제로 띄워 골든 패스(정책 체크·커스텀 추가/삭제·업로드 성공/거부)를 눌러가며 검증 — **CORS 버그를 실제로 이 과정에서 발견** |
| Bash (Gradle/Docker) | 컴파일·테스트·빌드 검증. `./gradlew test`, `docker build`, 컨테이너 스모크 테스트를 매 변경 후 실행해 "될 것 같다"가 아니라 "됐다"를 확인 |

## 판단 근거 회고

**AI가 놓쳤는데 사용자가 잡아낸 것**
- `LocalDateTime` 대신 `Instant`/`BaseTimeEntity` 사용 — 타임존 이슈를 AI가 먼저 짚지 못했고, 사용자가 실무 경험 기반으로 직접 지적한 뒤에야 반영됨. 이게 이 프로젝트에서 가장 명확한 "AI 결과를 검증 없이 안 쓴" 사례다.
- 참고 코드가 "다른 지원자의 제출물"이라는 맥락을 AI가 처음엔 몰랐고, 사용자가 명시적으로 알려준 뒤 접근 방식(리팩토링 → 전면 재작성) 자체가 바뀌었다.
- 화면을 2개로 나눌지 1개로 합칠지 — AI 제안(2개, 라우팅)보다 과제 원본 이미지에 더 부합하는 사용자 판단(1개)이 맞았다.

**AI가 스스로 찾아내고 사용자가 검증 후 채택한 것**
- PE/텍스트/ZIP 계열 매직 넘버 충돌과 그 해결 규칙
- `.env.local` 버그
- Null byte injection 방어 + DB 저장 시 이스케이프 필요성
- `boolean`→`Boolean` 침묵 실패
- CORS 설정 누락(실제 브라우저 테스트로 발견)

**그대로 안 쓰고 고쳐 쓴 것**
- Flyway → schema.sql (배포 속도 우선 판단으로 되돌림)
- "미지 바이너리는 항상 차단" → "미지 바이너리는 claimed 인정" (과잉 차단 방지)

**버린 것**
- S3 기반 파일 스토리지 제안 (스코프 밖으로 판단)
- 프론트 라우팅/탭 분리 제안 (원페이지가 더 적합)
