# AI 활용 기록

이 과제를 Claude Code(AI 코딩 에이전트)와 재미나이의 대화로 진행했습니다. 
아래는 판단 지점들을 정리한 기록입니다.

---

### Flyway 제거 — AI 판단을 사용자가 뒤집은 사례
프로젝트 스펙 설계시 AI가 Flyway 마이그레이션 + `schema.sql` 혼합 방식으로 제안했습니다.

> "코드는 엄격한 실무 기준으로 환경 쪽은 정확하고 빠른 배포가 우선이야 flyway는 필요 없는 스택이니 걷어내자"

AI 결과를 고쳐 쓴 사례: 위 판단에 따라 Flyway 의존성/마이그레이션 파일을 전부 제거하고 `schema.sql`+`data.sql` 방식으로 되돌렸습니다.

**판단 기준**: 이 과제는 테이블이 처음부터 끝까지 2개(`extension_policy`, `upload_file`)로 고정되어 있고, 배포 환경도 단일하고, 
단기 1인 프로젝트라 스키마 버전 이력 관리, 점진적인 스키마 변화 추적, 여러 개발자 간 마이그레이션 동기화 같은 Flyway의 기능이 발휘될 상황 자체가 없다고 판단했습니다. 
다만 AI가 이걸 제안한 이유가 실제 이 프로젝트의 필요보다는, 제가 이전에 다른 프로젝트에서 Flyway를 쓴 이력을 참고해 관성적으로 제안한 것 같다고 추측했습니다.
이번 프로젝트 규모엔 불필요하다고 판단해 제외시켰습니다.

### 고정/커스텀 확장자 테이블 구조 — 사용자가 설계하고 AI가 검수한 사례
고정 확장자와 커스텀 확장자를 별도 테이블(클래스)로 나눌지, 하나의 테이블에 `type` 컬럼으로 구분할지를 파악하고 검토했습니다.

> "현재 파일 확장자 차단 과제를 하고있는데 고정 확장자 커스텀 확장자 2개의 테이블로 분리하는거보다 1개의 테이블로 사용하고 Evum type으로 구분해서 사용하면 유니크 제약등을 이용해서 자동으로 두 확장자에서 겹치는 확장자를 자동으로 거를수있고 좋을거같은데 어때?"

**판단 기준**:테이블을 분리하면 서로 다른 두 테이블 사이의 값 충돌을 DB 유니크 제약으로 막을 방법이 없어 애플리케이션 코드에만 의존하게 되는 반면, 
하나의 테이블 + `type` 컬럼 방식은 `extension` 컬럼 하나에 대소문자 무시 유니크 인덱스를 걸어 고정/커스텀 간 충돌까지 한 번에 막을 수 있었다. 
이 트레이드오프를 근거로 후자로 결정했습니다.

### Bean Validation — 놓친 기본 실수를 검수 중 발견
고정 확장자 토글 요청 바디에 Bean Validation을 적용하면서도, blocked필드를 원시 타입 boolean으로 받고 있었다는 기본적인 실수를 처음엔 놓쳤습니다.
검수 및 테스트 과정에서 이 문제를 발견해 Boolean + @NotNull로 즉시 수정했습니다.

**판단 기준**:클라이언트가 blocked필드를 빠뜨렸을 때 Jackson이 false로 역직렬화해버려, 차단 해제를 요청한 적이 없는데도 필드 누락만으로 차단이 풀리는 문제가 생길 수 있음 

### 디자인 — 구체적 레퍼런스 제시, AI를 그대로 채택한 것
> "체크박스는 아이폰 스위치처럼 만들어줘 메인컬러는 #ac00ca 카드 + 그림자 기반 레이아웃으로 전체 리팩토링"

AI는 iOS 스타일 토글 스위치, 카드+그림자 기반 레이아웃, `::file-selector-button`을 이용한 네이티브 파일 입력 커스터마이징으로 대응했고, 나온 결과물을 채택했습니다.


### null byte injection 방어 — 사용자가 판단하고 AI가 검수·구현한 사례
파일명에 null byte(`\0`)가 섞인 경우(`shell.php\0.jpg`류의 업로드 우회 기법)를 즉시 거부하도록 방어했습니다. 
원본을 그대로 이력에 저장하면 PostgreSQL이 null byte를 포함한 문자열의 INSERT 자체를 거부한기 때문입니다.

> "postgre는 문자열 저장시 null값이 포함되면 문제를 일으키는데 이번 과제에서 어떤 식으로 처리하면될까?"

### `.gitignore` 누락 발견 — AI를 그대로 채택한 것
Dockerfile 작성 후 로컬에서 `docker build` + 컨테이너 기동 + API 응답까지 직접 검증하는 과정에서, `uploads-data/`가 `.gitignore`에 빠져 있어 로컬 테스트 파일이 커밋될 뻔한 것을 AI가 미리 발견해 제안했고, 채택했습니다.

### 컬럼 길이 초과로 인한 500 버그 — 검수 중 발견하고 함께 구현·검증
`extension`/`upload` 도메인 라인 단위 검수 중, `FileSignatureDetector`의 미지 바이너리 처리 경로에 확장자 길이 제한이 없다는 걸 발견했습니다.
근본 원인(`FilenameAnalyzer`의 길이 제한 부재)과 방어선(`UploadFile` 저장 직전 truncate) 수정하였습니다.

### 200개 상한 경쟁 상태 — AI 판단을 사용자가 뒤집은 사례
처음 구현 시 동시 요청에 대한 판단을 AI가 놓쳤고, advisory lock으로 해결했습니다. 자세한 내용은 CONSIDERATIONS.md에 정리했습니다.

### 모노레포 통합 — AI를 그대로 채택한 것
> "아냐 할게 더있어. GitHub 주소, table schema 포함, 실행 방법(README) 포함을 제출하는게 과제 요건인데 프론트엔드 따로 분리한게 좀 아닌거같아."

당시 구조를 바꾸기 가장 간단한 시점이라는 점을 고려해 모노레포 통합을 선택했습니다. 
프론트엔드 저장소의 커밋 히스토리 전체를 백엔드 저장소의 `frontend/` 하위 디렉터리로 병합하는 방법과 명령어를 제시했고,
명령어를 실행 및 병합 후 재검증까지 마쳤습니다.

**판단 기준**:아직 프로젝트 구조가 단순하다고 생각했고, 모노레포로 구조 개선을 진행하더라도 리스크는 적고, 리펙토링하는데 속도는 빠르다고 생각하여 AI의 제안을 채택했습니다.
---

## 사용한 스킬/도구/MCP

| 도구 | 용도 |
|---|---|
| Claude Code (Sonnet 5) | 전체 개발 세션 — 설계 논의, 코드 작성, 리뷰 |
| JetBrains MCP | IDE에 열린 프로젝트 상태(모듈, git 저장소, 디렉터리 구조) 확인 — 두 프로젝트(백엔드/프론트)가 동시에 열려 있을 때 프로젝트 경로 명시에 사용 |
| Claude Browser (Chrome 기반 자동화) | 프론트엔드를 실제로 띄워 골든 패스(정책 체크·커스텀 추가/삭제·업로드 성공/거부)를 눌러가며 검증 — **CORS 버그를 실제로 이 과정에서 발견** |
| Bash (Gradle/Docker) | 컴파일·테스트·빌드 검증. `./gradlew test`, `docker build`, 컨테이너 스모크 테스트, 동시성 통합테스트를 매 변경 후 실행해 "될 것 같다"가 아니라 "됐다"를 확인 |
| Gemini | 설계 및 구현 |
